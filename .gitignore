# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
build/
dist/

# Virtual environments
venv/
env/
.venv/

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
*.cover
coverage.xml

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Type checking
.mypy_cache/

# Rust
target/
**/*.rs.bk

# Parquet cache and pipeline outputs
causal_model/cache/
*.parquet
# ...except the committed full-warehouse export at the repo root
!/asset_metrics.parquet
causal_model/results.json
causal_model/results.csv

# MCP secrets
.claude/mcp.json

# Local DuckDB snapshots (regenerable via causal_portfolio.data.snapshot)
*.duckdb
*.duckdb.wal

# Crash dumps
*.stackdump

# Execution audit logs (real account activity, never commit)
causal_portfolio/execution/logs/
causal_portfolio/execution/state/

# Reference papers (PDFs kept locally, not in git)
references/
causal_portfolio/data/cache/*.parquet

# Operator DAG workspaces from the dashboard editor (local scratch)
causal_portfolio/data/dag_workspaces.json
