{
  "endpoint_id": "defillama_stablecoin_flow_daily",
  "description": "Historical stablecoin circulating supply on Ethereum — proxy for stable capital flows",
  "table": "asset_metrics",
  "primary_keys": ["provider", "asset", "metric", "time"],
  "date_range": {
    "start": "2021-01-01",
    "end": "2026-01-01"
  },
  "providers": [
    {
      "name": "defillama",
      "enabled": true,
      "priority": 1,
      "config": {
        "endpoint_type": "stablecoin/charts",
        "params": {
          "schema_type": "stablecoin_flow",
          "chain": "Ethereum",
          "asset": "stablecoins_ethereum"
        }
      }
    }
  ],
  "fallback_strategy": "use_all",
  "deduplication": {
    "strategy": "prefer_priority",
    "conflict_resolution": "keep_highest_priority"
  }
}
